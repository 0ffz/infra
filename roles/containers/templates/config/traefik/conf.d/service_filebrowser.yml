http:
  services:
    filebrowser:
      loadBalancer:
        servers:
          - url: "http://filebrowser:80"

  routers:
    filebrowser:
      rule: Host(`files.{{domain_services}}`)
      service: "filebrowser"
      middlewares:
        - sablier-long-session@file
